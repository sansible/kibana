---

kibana:
  host: 0.0.0.0
  install_dir: /usr/share/kibana4/
  port: 5601
  tarball: kibana-4.6.2-linux-x86_64
  download_base_url: https://download.elastic.co/kibana/kibana/
  # Used to determine which config file to use
  version: 4.6.2

  elasticsearch:
    host: elasticsearch.elk.internal
    port: 9200

  plugins: [ ]
  # Examples:
  #  - plugin_name: mobz/elasticsearch-head
  #    plugin_dir:  head
